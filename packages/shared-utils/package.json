{
  "name": "@efg/shared-utils",
  "version": "0.0.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && ./node_modules/.bin/tsc -p ./tsconfig.json",
    "clean": "rm -rf node_modules && rm -rf .turbo && rm -rf dist"
  },
  "files": [
    "dist/**"
  ],
  "dependencies": {},
  "devDependencies": {
    "@efg/tsconfig": "*",
    "typescript": "4.7.4"
  }
}
