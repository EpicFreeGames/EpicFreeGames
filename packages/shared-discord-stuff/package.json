{
  "name": "shared-discord-stuff",
  "version": "0.0.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && ./node_modules/.bin/tsc -p ./tsconfig.json",
    "clean": "rm -rf node_modules && rm -rf .turbo && rm -rf dist"
  },
  "files": [
    "dist/**"
  ],
  "dependencies": {
    "@crowdin/ota-client": "^0.7.0",
    "config": "*",
    "shared": "*",
    "database": "*",
    "discord-api-types": "^0.27.1",
    "discord.js": "^13.6.0",
    "i18next": "^21.8.1",
    "typescript": "^4.6.3"
  },
  "devDependencies": {
    "tsconfig": "*"
  }
}
