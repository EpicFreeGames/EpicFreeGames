{
  "name": "efg-mono",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run development",
    "devr": "yarn clean && yarn && yarn build && yarn dev",
    "postclean": "rm -rf node_modules",
    "clean": "turbo run clean",
    "api::dev": "turbo run build --filter=api... && turbo run dev --filter=api...",
    "ie::dev": "turbo run build --filter=interactions-endpoint... && turbo run dev --filter=interactions-endpoint..."
  },
  "devDependencies": {
    "turbo": "1.4.4",
    "@trivago/prettier-plugin-sort-imports": "^3.3.0",
    "dotenv-cli": "^6.0.0",
    "prettier": "^2.7.1",
    "prettier-plugin-tailwindcss": "^0.1.13"
  },
  "dependencies": {},
  "engines": {
    "npm": ">=8.6.0",
    "node": ">=18.0.0"
  },
  "packageManager": "yarn@1.22.19"
}
