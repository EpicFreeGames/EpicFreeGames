{
  "name": "freegames-bot",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "postclean": "rm -rf node_modules",
    "clean": "turbo run clean",
    "depl": "turbo run depl",
    "depl-g": "turbo run depl-g",
    "devup": "docker-compose -p dev up -d --build",
    "devdown": "docker-compose -p dev down",
    "produp": "docker-compose -p prod up -d --build",
    "proddown": "docker-compose -p prod down",
    "onlydb": "docker-compose -p dev up -d efg-db --build"
  },
  "devDependencies": {
    "turbo": "^1.1.2"
  },
  "engines": {
    "npm": ">=7.0.0",
    "node": ">=14.0.0"
  },
  "packageManager": "yarn@1.22.17"
}
