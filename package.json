{
  "name": "freegames-bot",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "start": "turbo run start",
    "build": "yarn && turbo run build",
    "dev": "turbo run dev",
    "devr": "yarn clean && yarn && yarn build && yarn dev",
    "dev:front": "yarn build --scope=web-ui && turbo run dev --scope=web-ui",
    "build:front": "turbo run build --scope=web-ui && turbo run buildprod --scope=web-ui",
    "start:front": "turbo run start --scope=web-ui",
    "postclean": "rm -rf node_modules",
    "clean": "turbo run clean",
    "reg": "turbo run reg",
    "reg-g": "turbo run reg-g",
    "up": "docker-compose up -d --build",
    "down": "docker-compose down"
  },
  "devDependencies": {
    "turbo": "^1.2.4"
  },
  "engines": {
    "npm": ">=8.5.5",
    "node": ">=16.5.0"
  },
  "packageManager": "yarn@1.22.19"
}
