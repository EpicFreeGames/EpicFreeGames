{
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"],
      "cache": false
    },
    "buildprod": {
      "dependsOn": ["^build", "^buildprod"],
      "outputs": ["dist/**", ".next/**"],
      "cache": false
    },
    "dev": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**"],
      "cache": false
    },
    "start": {
      "dependsOn": ["^build", "^buildprod", "build", "^start"],
      "cache": false
    },
    "typecheck": {
      "dependsOn": ["^build", "build", "^typecheck"],
      "cache": false
    }
  },
  "globalEnv": ["ENV", "VERSION"]
}
