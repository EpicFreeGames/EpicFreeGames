{
  "name": "@efg/interactions-endpoint",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && ./node_modules/.bin/tsc -p ./tsconfig.json",
    "dev": "nodemon -q -e ts,json --ignore ./dist --watch ./ --watch ../../packages --exec 'yarn build && node ./dist/index.js'",
    "start": "node ./dist/index.js",
    "clean": "rm -rf node_modules && rm -rf .turbo && rm -rf dist"
  },
  "dependencies": {
    "@efg/configuration": "*",
    "@efg/embeds": "*",
    "@efg/i18n": "*",
    "@efg/types": "*",
    "discord-api-types": "^0.37.7",
    "discord-interactions": "^2.4.1",
    "express": "^4.17.2"
  },
  "devDependencies": {
    "@efg/tsconfig": "*",
    "@types/express": "^4.17.13",
    "nodemon": "^2.0.19",
    "typescript": "4.7.4"
  }
}
