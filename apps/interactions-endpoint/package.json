{
  "name": "@efg/interactions-endpoint",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && ./node_modules/.bin/tsc -p ./tsconfig.json",
    "start": "node --experimental-specifier-resolution=node ./dist/index.js",
    "dev": "nodemon -q -e ts,json --ignore ./dist --watch ./ --exec 'yarn build && yarn start'"
  },
  "dependencies": {
    "@efg/configuration": "*",
    "@efg/embeds": "*",
    "@efg/i18n": "*",
    "@efg/types": "*",
    "@efg/logger": "*",
    "@efg/shared-utils": "*",
    "discord-api-types": "^0.37.7",
    "discord-interactions": "^2.4.1",
    "express": "^4.17.2"
  },
  "devDependencies": {
    "@efg/tsconfig": "*",
    "@types/express": "^4.17.13",
    "nodemon": "^2.0.19",
    "typescript": "4.7.4"
  }
}
